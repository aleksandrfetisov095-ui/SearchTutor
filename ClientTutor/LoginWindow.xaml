<Window x:Class="ClientTutor.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClientTutor"
        mc:Ignorable="d"
        Title="Вход в систему" Height="300" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text=" Вход в SearchTutor" 
                   FontSize="18" FontWeight="Bold" 
                   Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Text="Email:" Margin="0,5"/>
        <TextBox Grid.Row="2" x:Name="EmailBox" 
                 Height="30" Padding="5" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="3" Text="Пароль:" Margin="0,5"/>
        <PasswordBox Grid.Row="4" x:Name="PasswordBox" 
                     Height="30" Padding="5" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Войти" 
                    Click="LoginButton_Click"
                    Padding="20,8" Margin="0,0,10,0"
                    Background="Blue" Foreground="White"
                    BorderThickness="0" Cursor="Hand"
                    IsDefault="True"/>

            <Button Content="Отмена" 
                    Click="CancelButton_Click"
                    Padding="20,8"
                    Background="Red" Foreground="White"
                    BorderThickness="0" Cursor="Hand"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
